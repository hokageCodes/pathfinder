/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'img.freepik.com', 'i.pinimg.com', 'media.istockphoto.com'],
  },
};

export default nextConfig;
